#!/bin/bash
cd "$(git rev-parse --show-toplevel)"
if git diff --cached --name-only | grep -q "slides.md"; then
    prettier slides.md -w
    marp slides.md -o slides.pdf
	git add slides.md slides.pdf
fi
